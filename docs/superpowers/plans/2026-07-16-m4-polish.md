# M4: 폴리시 (URL 상태 + 이관 항목) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 뷰 상태(모드·키·스케일/코드·라벨·박스)를 URL 쿼리로 북마크할 수 있게 하고, M3 최종 리뷰가 이관한 견고성·UX·접근성·위생 항목을 정리한다 (docs/PLAN.md F5 + §10 M3→M4 이관).

**Architecture:** URL 직렬화/파싱은 순수 모듈(`src/lib/viewUrl.ts`)로 분리해 TDD하고, page는 마운트 시 파싱·변경 시 `history.replaceState`로 동기화한다. 퀴즈 라운드 기록은 파생 상태(roundOver) 전이를 감지하는 이펙트로 옮기고(ref 가드로 1회 보장), 진행 상태 변경은 함수형 setState로 통일한다. 범위 시각화는 `Fretboard`에 `activeRegion` prop(범위 밖 덮개 렌더)으로 추가한다.

**Tech Stack:** 기존과 동일. 신규 런타임 의존성 금지. **다크 모드는 사용자 결정으로 이번 범위에서 제외.**

## Global Constraints

- M1~M3 Global Constraints 전부 유지 (한국어 UI, aria-pressed, conventional commits, 기존 테스트 보존)
- **URL 상태 범위 (확정)**: 스케일/코드톤 뷰 상태만 인코딩 — `mode`(scale 기본·생략), `key`, `scale`, `chord`, `label`, `box`(1-based). 퀴즈는 `mode=quiz`(탭 복원)만, 내부 설정은 비저장. 기본값과 같은 파라미터는 생략. 잘못된 값은 기본값으로 폴백
- **미답 문제 스킵 (확정)**: 모드 A에서 "다음 문제"로 미답 스킵 허용 유지 — 기록 없음. 의도적 결정이므로 코드 주석으로 문서화
- 퀴즈 라운드 기록은 라운드당 정확히 1회 (이펙트 + ref 가드, StrictMode 이중 마운트에도 안전)
- 출제 범위 변경(현 토글·프렛 셀렉트)은 진행 중 문제/라운드를 초기화
- 히트 타겟 키보드 조작: role="button", tabIndex=0, aria-label "{n}번 줄 {f}프렛", Enter/Space 동작

## File Structure

```
src/quiz/stats.ts            — 수정: setItem try/catch (Task 1)
src/theory/chords.test.ts    — 수정: CHORD_IDS 완전성 테스트 (Task 1)
src/quiz/engine.test.ts      — 수정: fretMax<0 테스트 (Task 1)
src/components/Quiz.tsx      — 수정: FretPos 재사용·select id·세션 모드별 분리·.quiz 제거 (Task 1),
                                      이펙트 기반 기록·함수형 setState·범위 변경 리셋 (Task 2),
                                      activeRegion 전달 (Task 3)
src/components/Controls.tsx  — 수정: select id 부여 (Task 1)
src/components/Fretboard.tsx — 수정: activeRegion (Task 3), 키보드 접근성 (Task 4)
src/lib/viewUrl.ts           — 신규: parseViewQuery / viewQueryString (Task 5)
app/page.tsx                 — 수정: URL 동기화 (Task 5)
app/globals.css              — 수정: focus-visible 아웃라인 (Task 4)
```

---

### Task 1: M3 이관 소품 배치

**Files:**
- Modify: `src/quiz/stats.ts`, `src/components/Quiz.tsx`, `src/components/Controls.tsx`
- Test: `src/quiz/stats.test.ts`, `src/theory/chords.test.ts`, `src/quiz/engine.test.ts`, `src/components/Quiz.test.tsx`

**Interfaces:**
- Produces: 동작 변경은 ① setItem 실패 무해화 ② 세션 카운터 모드별 분리 두 가지. 나머지는 테스트·id·타입 위생

- [ ] **Step 1: 실패하는 테스트 작성**

`src/quiz/stats.test.ts`에 추가 (`vi`를 vitest import에 병합):

```ts
  it("does not throw when storage write fails", () => {
    const spy = vi.spyOn(Storage.prototype, "setItem").mockImplementation(() => {
      throw new Error("quota");
    });
    const s = recordResult("nameThatNote", true, 100);
    expect(s.nameThatNote).toEqual({ attempts: 1, correct: 1, totalMs: 100 });
    spy.mockRestore();
  });
```

`src/theory/chords.test.ts`의 CHORDS describe에 추가:

```ts
  it("CHORD_IDS covers every chord in CHORDS", () => {
    expect([...CHORD_IDS].sort()).toEqual(Object.keys(CHORDS).sort());
  });
```

`src/quiz/engine.test.ts`의 makeNameQuestion describe에 추가:

```ts
  it("negative fretMax yields an empty range and throws", () => {
    expect(() => makeNameQuestion({ strings: [6], fretMax: -1 })).toThrow("퀴즈 범위에 위치가 없습니다");
  });
```

`src/components/Quiz.test.tsx`에 추가 (기존 stubQuestion·stubTarget 사용, 최상위 describe 밖 아무 describe에나):

```tsx
describe("Quiz — 세션 카운터", () => {
  it("keeps session counters per quiz mode", () => {
    const { getByRole, getByText } = render(
      <Quiz makeQuestion={() => stubQuestion} makeTarget={() => stubTarget} />
    );
    fireEvent.click(getByRole("button", { name: "시작" }));
    fireEvent.click(getByRole("button", { name: "A" }));
    expect(getByText(/이번 세션 1\/1/)).not.toBeNull();
    fireEvent.click(getByRole("button", { name: "모두 찾기" }));
    expect(getByText(/이번 세션 0\/0/)).not.toBeNull();
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npm test`
Expected: setItem 테스트(현재 throw 전파)와 세션 카운터 테스트(현재 공유 카운터 1/1 표시) FAIL. 완전성·fretMax 테스트는 PASS일 수 있음(커버리지 핀).

- [ ] **Step 3: 구현**

`src/quiz/stats.ts` — `recordResult`의 저장부를 교체:

```ts
  if (typeof window !== "undefined") {
    try {
      window.localStorage.setItem(STORAGE_KEY, JSON.stringify(stats));
    } catch {
      // 저장 실패(쿼터/프라이빗 모드)는 통계 기능 저하로만 취급 — UI 흐름을 깨지 않는다
    }
  }
```

`src/components/Quiz.tsx` 수정 4곳:

1. import에 `type FretPos` 추가 (`@/theory/fretboard`에서), `handlePositionClick`의 인라인 파라미터 타입 `{ str: StringNo; fret: number }`를 `FretPos`로 교체. `StringNo` import가 여전히 쓰이는지 확인(현 토글에서 사용 — 유지)
2. 세션 상태를 모드별로 분리:

```tsx
  const [session, setSession] = useState({
    nameThatNote: { asked: 0, correct: 0 },
    findAll: { asked: 0, correct: 0 },
  });
```

`answer()`의 세션 갱신을:

```tsx
    setSession((s) => ({
      ...s,
      nameThatNote: {
        asked: s.nameThatNote.asked + 1,
        correct: s.nameThatNote.correct + (correct ? 1 : 0),
      },
    }));
```

`handlePositionClick` 완료 분기와 `giveUp()`의 세션 갱신을 (완료 분기 예시):

```tsx
        setSession((s) => ({
          ...s,
          findAll: { asked: s.findAll.asked + 1, correct: s.findAll.correct + (clean ? 1 : 0) },
        }));
```

(giveUp은 `correct: s.findAll.correct` 그대로.) 통계 줄 표시를 `session[quizMode]` 기준으로:

```tsx
        이번 세션 {session[quizMode].correct}/{session[quizMode].asked}
```

3. `<section className="board quiz">`의 데드 클래스 제거 → `className="board"`
4. 프렛 범위 `<select>`에 `id="quiz-fret-max"` 부여

`src/components/Controls.tsx` — 세 `<select>`에 id 부여: 키(루트) `id="view-key"`, 스케일 `id="view-scale"`, 코드 `id="view-chord"`

- [ ] **Step 4: 테스트 통과 확인**

Run: `npm test` → Expected: 81개 전부 PASS (77 + 신규 4)
Run: `npm run build` → Expected: 성공

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "chore: M3 review carry-overs (storage guard, per-mode session, hygiene)"
```

---

### Task 2: 퀴즈 라운드 견고화 (이펙트 기반 기록 + 범위 변경 리셋)

**Files:**
- Modify: `src/components/Quiz.tsx`
- Test: `src/components/Quiz.test.tsx`

**Interfaces:**
- Consumes: Task 1의 모드별 세션
- Produces: 외부 인터페이스 변화 없음. 내부: `recordedRef` 1회 기록 가드, `resetProgress()` 공용 리셋, found/misses 함수형 setState

**설계 배경:** M3 검증에서 프로그래매틱 배칭 클릭 시 렌더 클로저의 `found`를 읽는 `setFound(next)`가 앞선 클릭을 유실하는 것이 관측됐다(실사용 discrete 이벤트에선 미발생). 함수형 업데이트로 바꾸면 완료 판정(`next.size === ...`)을 핸들러에서 할 수 없으므로, 기록을 렌더 파생 상태(`roundOver`) 전이를 감지하는 이펙트로 옮긴다. 이펙트는 updater 순수성 문제가 없고, ref 가드로 라운드당 1회를 보장한다 (recordResult는 updater 안에서 호출 금지 — StrictMode에서 이중 기록됨).

- [ ] **Step 1: 실패하는 테스트 작성**

`src/components/Quiz.test.tsx`의 "Quiz — 모두 찾기" describe에 추가:

```tsx
  it("resets an active round when the range changes", () => {
    const { getByRole, getByLabelText, container } = setup();
    fireEvent.click(container.querySelector("[data-testid='hit-6-5']")!);
    expect(container.querySelector("[data-testid='mark-6-5']")).not.toBeNull();
    fireEvent.change(getByLabelText("프렛 범위"), { target: { value: "5" } });
    expect(container.querySelector("[data-testid='mark-6-5']")).toBeNull();
    expect(container.querySelector(".view-title")?.textContent).toBe("퀴즈 — 모두 찾기");
    expect(getByRole("button", { name: "시작" })).not.toBeNull();
  });

  it("records a completed round exactly once even after further re-renders", () => {
    const { getByRole, container } = setup();
    fireEvent.click(container.querySelector("[data-testid='hit-6-5']")!);
    fireEvent.click(container.querySelector("[data-testid='hit-5-0']")!);
    // 완료 후 추가 상태 변화(현 토글 → 리셋 경로)에도 중복 기록 없어야 함
    fireEvent.click(getByRole("button", { name: "4번" }));
    const raw = window.localStorage.getItem("fretboard-quiz-stats-v1");
    expect(JSON.parse(raw!).findAll.attempts).toBe(1);
  });
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npm test -- src/components/Quiz.test.tsx`
Expected: 범위 변경 리셋 테스트 FAIL (현재 범위 변경이 라운드를 유지)

- [ ] **Step 3: 구현**

`src/components/Quiz.tsx` — import에 `useRef` 추가. 변경:

1. 공용 리셋과 ref 가드:

```tsx
  const recordedRef = useRef(false);

  const resetProgress = () => {
    setQuestion(null);
    setPicked(null);
    setTarget(null);
    setFound(new Set());
    setMisses(new Set());
    setRevealed(false);
    recordedRef.current = false;
  };
```

`switchQuizMode`의 개별 리셋 5줄을 `resetProgress()` 호출로 교체.

2. 범위 변경 시 리셋 — `toggleString`을 다음으로 교체하고, 프렛 범위 select의 onChange를 `changeFretMax` 호출로 교체:

```tsx
  const toggleString = (s: StringNo) => {
    const has = range.strings.includes(s);
    if (has && range.strings.length === 1) return; // 최소 1현 유지
    setRange({
      ...range,
      strings: has ? range.strings.filter((x) => x !== s) : [...range.strings, s],
    });
    resetProgress();
  };

  const changeFretMax = (fretMax: number) => {
    setRange((r) => ({ ...r, fretMax }));
    resetProgress();
  };
```

3. `startFind`에 `recordedRef.current = false;` 추가, 세션/통계 기록 코드를 핸들러에서 제거하고 함수형 setState로:

```tsx
  const handlePositionClick = (pos: FretPos) => {
    if (!target || roundOver) return;
    const k = posKey(pos);
    if (found.has(k) || misses.has(k)) return;
    if (targetKeys!.has(k)) {
      setFound((prev) => new Set(prev).add(k));
    } else {
      setMisses((prev) => new Set(prev).add(k));
    }
  };

  const giveUp = () => {
    if (!target || roundOver) return;
    setRevealed(true);
  };
```

4. 기록 이펙트 (`roundComplete`/`roundOver` 파생 선언 아래에):

```tsx
  useEffect(() => {
    if (!target || !roundOver || recordedRef.current) return;
    recordedRef.current = true;
    const clean = roundComplete && misses.size === 0;
    setSession((s) => ({
      ...s,
      findAll: { asked: s.findAll.asked + 1, correct: s.findAll.correct + (clean ? 1 : 0) },
    }));
    setStats(recordResult("findAll", clean, Date.now() - askedAt));
  }, [target, roundOver, roundComplete, misses, askedAt]);
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npm test` → Expected: 83개 전부 PASS (81 + 신규 2). 기존 모두 찾기 완료·포기 테스트가 이펙트 경로로도 그대로 통과하는지 특히 확인
Run: `npm run build` → Expected: 성공

- [ ] **Step 5: Commit**

```bash
git add src/components/Quiz.tsx src/components/Quiz.test.tsx
git commit -m "refactor: effect-based round recording and range-change reset in quiz"
```

---

### Task 3: 퀴즈 범위 보드 시각 표시 (`activeRegion`)

**Files:**
- Modify: `src/components/Fretboard.tsx`, `src/components/Quiz.tsx`
- Test: `src/components/Fretboard.test.tsx`, `src/components/Quiz.test.tsx`

**Interfaces:**
- Produces: `FretboardProps.activeRegion?: { strings: readonly StringNo[]; fretMax: number } | null` — 범위 밖 영역에 반투명 덮개 렌더 (`pointer-events: none`)

- [ ] **Step 1: 실패하는 테스트 작성**

`src/components/Fretboard.test.tsx`의 quiz interaction describe에 추가:

```tsx
  it("dims the board outside the active region", () => {
    const { container } = render(
      <Fretboard notes={empty} labelMode="none" window={null}
                 activeRegion={{ strings: [6, 5], fretMax: 12 }} />
    );
    expect(container.querySelector("[data-testid='region-fret-cover']")).not.toBeNull();
    expect(container.querySelector("[data-testid='region-string-cover-1']")).not.toBeNull();
    expect(container.querySelector("[data-testid='region-string-cover-6']")).toBeNull();
  });

  it("renders no overlay when the region covers the whole board", () => {
    const { container } = render(
      <Fretboard notes={empty} labelMode="none" window={null}
                 activeRegion={{ strings: [1, 2, 3, 4, 5, 6], fretMax: 22 }} />
    );
    expect(container.querySelector("[data-testid='region-fret-cover']")).toBeNull();
    expect(container.querySelector("[data-testid^='region-string-cover']")).toBeNull();
  });
```

`src/components/Quiz.test.tsx`에 추가 (세션 카운터 describe 옆):

```tsx
describe("Quiz — 범위 표시", () => {
  it("shows the quiz range on the board", () => {
    const { container } = render(<Quiz />);
    // 기본 범위(6·5번 줄) 밖인 1번 줄에 덮개
    expect(container.querySelector("[data-testid='region-string-cover-1']")).not.toBeNull();
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npm test -- src/components/Fretboard.test.tsx src/components/Quiz.test.tsx`
Expected: FAIL — `activeRegion` prop 없음

- [ ] **Step 3: 구현**

`src/components/Fretboard.tsx`:

1. props 추가:

```tsx
  activeRegion?: { strings: readonly StringNo[]; fretMax: number } | null;
```

시그니처 destructuring에 `activeRegion = null` 추가.

2. 프렛 번호 레이어와 노트 레이어 사이에 덮개 레이어 삽입:

```tsx
      {/* 출제 범위 밖 덮개 */}
      {activeRegion && (
        <g data-testid="active-region" pointerEvents="none">
          {activeRegion.fretMax < FRET_COUNT && (
            <rect data-testid="region-fret-cover"
                  x={fretX(Math.max(activeRegion.fretMax, 0))} y={TOP_Y - 16}
                  width={W - RIGHT_PAD - fretX(Math.max(activeRegion.fretMax, 0))}
                  height={STRING_GAP * 5 + 32}
                  fill="var(--bg)" opacity={0.6} />
          )}
          {STRINGS.filter((s) => !activeRegion.strings.includes(s)).map((s) => (
            <rect key={s} data-testid={`region-string-cover-${s}`}
                  x={OPEN_X - 14} y={stringY(s) - STRING_GAP / 2}
                  width={W - OPEN_X} height={STRING_GAP}
                  fill="var(--bg)" opacity={0.6} />
          ))}
        </g>
      )}
```

`src/components/Quiz.tsx` — `<Fretboard ...>`에 `activeRegion={range}` 추가 (퀴즈 화면에서는 항상 표시 — 설정 중에도 범위가 보이는 것이 목적).

- [ ] **Step 4: 테스트 통과 확인**

Run: `npm test` → Expected: 86개 전부 PASS (83 + 신규 3)
Run: `npm run build` → Expected: 성공

- [ ] **Step 5: Commit**

```bash
git add src/components/Fretboard.tsx src/components/Fretboard.test.tsx src/components/Quiz.tsx src/components/Quiz.test.tsx
git commit -m "feat: visualize quiz range on the fretboard"
```

---

### Task 4: 히트 타겟 키보드 접근성

**Files:**
- Modify: `src/components/Fretboard.tsx`, `app/globals.css`
- Test: `src/components/Fretboard.test.tsx`

**Interfaces:**
- Produces: 히트 타겟이 `role="button"`, `tabIndex={0}`, `aria-label="{n}번 줄 {f}프렛"`, Enter/Space로 동작. svg 루트 role은 interactive 시 `"group"`

- [ ] **Step 1: 실패하는 테스트 작성**

`src/components/Fretboard.test.tsx`의 quiz interaction describe에 추가:

```tsx
  it("supports keyboard activation of click targets", () => {
    const onClick = vi.fn();
    const { container } = render(
      <Fretboard notes={empty} labelMode="none" window={null} interactive onPositionClick={onClick} />
    );
    const hit = container.querySelector("[data-testid='hit-6-5']")!;
    expect(hit.getAttribute("role")).toBe("button");
    expect(hit.getAttribute("tabindex")).toBe("0");
    expect(hit.getAttribute("aria-label")).toBe("6번 줄 5프렛");
    fireEvent.keyDown(hit, { key: "Enter" });
    expect(onClick).toHaveBeenCalledWith({ str: 6, fret: 5 });
  });

  it("switches the svg role when interactive", () => {
    const { container: plain } = render(
      <Fretboard notes={empty} labelMode="none" window={null} />
    );
    expect(plain.querySelector("svg")?.getAttribute("role")).toBe("img");
    const { container: inter } = render(
      <Fretboard notes={empty} labelMode="none" window={null} interactive />
    );
    expect(inter.querySelector("svg")?.getAttribute("role")).toBe("group");
  });
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npm test -- src/components/Fretboard.test.tsx`
Expected: FAIL — role/tabindex/aria-label 없음, svg role 고정 "img"

- [ ] **Step 3: 구현**

`src/components/Fretboard.tsx`:

1. svg 루트: `role="img"`를 `role={interactive ? "group" : "img"}`로 교체 (aria-label "기타 지판"은 유지)
2. 히트 타겟 `<circle>`에 속성 추가:

```tsx
                  role="button" tabIndex={0}
                  aria-label={`${str}번 줄 ${fret}프렛`}
                  onKeyDown={(e) => {
                    if (e.key === "Enter" || e.key === " ") {
                      e.preventDefault();
                      onPositionClick?.({ str, fret });
                    }
                  }}
```

`app/globals.css` 끝에 추가:

```css
.fretboard [role="button"] { outline: none; }
.fretboard [role="button"]:focus-visible {
  outline: 2px solid var(--accent);
  outline-offset: 2px;
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npm test` → Expected: 88개 전부 PASS (86 + 신규 2)
Run: `npm run build` → Expected: 성공

- [ ] **Step 5: Commit**

```bash
git add src/components/Fretboard.tsx src/components/Fretboard.test.tsx app/globals.css
git commit -m "feat: keyboard-accessible quiz click targets"
```

---

### Task 5: URL 상태 인코딩 (`viewUrl.ts` + page 동기화)

**Files:**
- Create: `src/lib/viewUrl.ts`
- Test: `src/lib/viewUrl.test.ts`
- Modify: `app/page.tsx`

**Interfaces:**
- Consumes: `KEYS`/`Key`, `SCALE_IDS`/`ScaleId`, `CHORD_IDS`/`ChordId`, `LabelMode`와 동일한 유니언
- Produces:
  - `interface UrlViewState { mode; keySel; scaleId; chordId; labelMode; boxIndex }` (page의 ViewState와 동일 형태)
  - `parseViewQuery(search: string, defaults: UrlViewState): UrlViewState` — 잘못된 값은 필드별 기본값 폴백, box는 1-based → 0-based
  - `viewQueryString(view: UrlViewState, defaults: UrlViewState): string` — 기본값 파라미터 생략, scale 모드에서만 scale/box, chord 모드에서만 chord, 결과 없으면 ""

- [ ] **Step 1: 실패하는 테스트 작성**

`src/lib/viewUrl.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { parseViewQuery, viewQueryString, type UrlViewState } from "./viewUrl";

const D: UrlViewState = {
  mode: "scale",
  keySel: "A",
  scaleId: "minorPentatonic",
  chordId: "7",
  labelMode: "name",
  boxIndex: null,
};

describe("viewQueryString", () => {
  it("returns empty string for the default view", () => {
    expect(viewQueryString(D, D)).toBe("");
  });

  it("encodes only non-default fields", () => {
    expect(viewQueryString({ ...D, mode: "chord", keySel: "E" }, D)).toBe("?mode=chord&key=E");
  });

  it("encodes box 1-based and only in scale mode", () => {
    expect(viewQueryString({ ...D, boxIndex: 1 }, D)).toBe("?box=2");
    expect(viewQueryString({ ...D, mode: "chord", boxIndex: 1 }, D)).toBe("?mode=chord");
  });

  it("omits scale in chord mode and chord in scale mode", () => {
    expect(viewQueryString({ ...D, scaleId: "blues" }, D)).toBe("?scale=blues");
    expect(viewQueryString({ ...D, mode: "chord", chordId: "maj7" }, D)).toBe("?mode=chord&chord=maj7");
    expect(viewQueryString({ ...D, chordId: "maj7" }, D)).toBe(""); // scale 모드에선 chord 생략
  });
});

describe("parseViewQuery", () => {
  it("round-trips a non-default view", () => {
    const v: UrlViewState = { ...D, mode: "chord", keySel: "Bb", chordId: "maj7" };
    expect(parseViewQuery(viewQueryString(v, D), D)).toEqual(v);
  });

  it("falls back to defaults on invalid values", () => {
    expect(parseViewQuery("?mode=nope&key=H&scale=phrygian&box=99", D)).toEqual(D);
  });

  it("parses box to 0-based index within 1..5", () => {
    expect(parseViewQuery("?box=3", D).boxIndex).toBe(2);
    expect(parseViewQuery("?box=0", D).boxIndex).toBeNull();
    expect(parseViewQuery("?box=6", D).boxIndex).toBeNull();
  });

  it("restores quiz mode from the URL", () => {
    expect(parseViewQuery("?mode=quiz", D).mode).toBe("quiz");
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npm test -- src/lib/viewUrl.test.ts`
Expected: FAIL — `Cannot find module './viewUrl'`

- [ ] **Step 3: 모듈 구현**

`src/lib/viewUrl.ts`:

```ts
import { KEYS, type Key } from "@/theory/notes";
import { SCALE_IDS, type ScaleId } from "@/theory/scales";
import { CHORD_IDS, type ChordId } from "@/theory/chords";

export type ViewMode = "scale" | "chord" | "quiz";
export type ViewLabelMode = "name" | "degree" | "none";

export interface UrlViewState {
  mode: ViewMode;
  keySel: Key;
  scaleId: ScaleId;
  chordId: ChordId;
  labelMode: ViewLabelMode;
  boxIndex: number | null;
}

const MODES: readonly ViewMode[] = ["scale", "chord", "quiz"];
const LABELS: readonly ViewLabelMode[] = ["name", "degree", "none"];

function oneOf<T extends string>(value: string | null, allowed: readonly T[]): T | null {
  return value !== null && (allowed as readonly string[]).includes(value) ? (value as T) : null;
}

export function parseViewQuery(search: string, defaults: UrlViewState): UrlViewState {
  const p = new URLSearchParams(search);
  const boxRaw = p.get("box");
  const box = boxRaw === null ? NaN : Number(boxRaw);
  return {
    mode: oneOf(p.get("mode"), MODES) ?? defaults.mode,
    keySel: oneOf(p.get("key"), KEYS) ?? defaults.keySel,
    scaleId: oneOf(p.get("scale"), SCALE_IDS) ?? defaults.scaleId,
    chordId: oneOf(p.get("chord"), CHORD_IDS) ?? defaults.chordId,
    labelMode: oneOf(p.get("label"), LABELS) ?? defaults.labelMode,
    boxIndex: Number.isInteger(box) && box >= 1 && box <= 5 ? box - 1 : null,
  };
}

export function viewQueryString(view: UrlViewState, defaults: UrlViewState): string {
  const p = new URLSearchParams();
  if (view.mode !== defaults.mode) p.set("mode", view.mode);
  if (view.keySel !== defaults.keySel) p.set("key", view.keySel);
  if (view.mode === "scale" && view.scaleId !== defaults.scaleId) p.set("scale", view.scaleId);
  if (view.mode === "chord" && view.chordId !== defaults.chordId) p.set("chord", view.chordId);
  if (view.labelMode !== defaults.labelMode) p.set("label", view.labelMode);
  if (view.mode === "scale" && view.boxIndex !== null) p.set("box", String(view.boxIndex + 1));
  const s = p.toString();
  return s ? `?${s}` : "";
}
```

- [ ] **Step 4: 모듈 테스트 통과 확인**

Run: `npm test -- src/lib/viewUrl.test.ts` → Expected: PASS (9개)

- [ ] **Step 5: page 동기화**

`app/page.tsx` 수정:

1. import 추가: `import { useEffect } from "react";`(기존 useState import에 병합), `import { parseViewQuery, viewQueryString, type UrlViewState } from "@/lib/viewUrl";`
2. 기존 `interface ViewState {...}` 선언을 제거하고 `UrlViewState`를 사용. 기본값을 상수로 추출:

```tsx
const DEFAULT_VIEW: UrlViewState = {
  mode: "scale",
  keySel: "A",
  scaleId: "minorPentatonic",
  chordId: "7",
  labelMode: "name",
  boxIndex: null,
};
```

`useState<UrlViewState>(DEFAULT_VIEW)`로 초기화.

3. 동기화 이펙트 2개 (컴포넌트 안):

```tsx
  // 마운트 시 URL → 상태 (SSR/하이드레이션 후 1회)
  useEffect(() => {
    setView(parseViewQuery(window.location.search, DEFAULT_VIEW));
  }, []);

  // 상태 → URL (북마크 가능, 히스토리 오염 없이 replace)
  useEffect(() => {
    const q = viewQueryString(view, DEFAULT_VIEW);
    window.history.replaceState(null, "", q || window.location.pathname);
  }, [view]);
```

4. `boxIndex`가 URL에서 복원됐지만 스케일이 박스 미지원인 경우는 기존 `boxes` null 가드가 이미 무해화함 — 변경 불필요 (주석 불필요)

- [ ] **Step 6: 전체 검증 + 커밋**

Run: `npm test` → Expected: 97개 전부 PASS (88 + 신규 9)
Run: `npm run build` → Expected: 성공 (useSearchParams를 쓰지 않으므로 Suspense 불필요 — window 접근은 전부 이펙트 안)

```bash
git add -A
git commit -m "feat: encode view state in the URL for bookmarking"
```

---

## Self-Review 결과

- **Spec coverage:** F5(URL 상태) → Task 5. §10 M3→M4 이관 — 동작 개선 3건(이펙트 기록+함수형 setState → Task 2, 범위 변경 리셋 → Task 2, setItem try/catch → Task 1), UX 6건(범위 시각 표시 → Task 3, 키보드+role → Task 4, 세션 분리·select id·.quiz 제거 → Task 1, 스킵 정책 → Global Constraints에 '허용 유지' 확정), 코드 위생 3건(FretPos·완전성·fretMax<0 → Task 1), 설계 노트(퀴즈 URL 리프트) → 범위 밖 확정(퀴즈 내부 설정 비저장). 다크 모드 → 사용자 결정으로 제외.
- **Placeholder scan:** 통과 — 전 스텝 실제 코드/명령 포함.
- **Type consistency:** `activeRegion` 형태(Task 3 정의 = QuizRange 구조와 동일 — Quiz가 range를 그대로 전달), `UrlViewState`(Task 5)는 page의 기존 ViewState와 필드 동일(mode 유니언에 "quiz" 포함), `FretPos`(Task 1에서 Quiz가 재사용). 테스트 수 누계: 77 → 81 → 83 → 86 → 88 → 97.
- **주의:** Task 2의 기록 이펙트는 `roundComplete`가 파생값이므로 deps에 포함, `recordedRef`는 ref라 deps 불필요. Task 5의 mount 이펙트는 빈 deps로 1회만 — StrictMode 이중 실행에도 같은 URL을 파싱하므로 멱등.
